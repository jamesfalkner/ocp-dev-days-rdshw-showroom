= Module 1: The developer experience
include::partial$_attributes.adoc[]
:imagesdir: ../assets/images

It is your first day at Parasol Insurance. You have been assigned to the claims processing team and given access to the company's application platform. Your manager has asked you to add a new claims statistics API endpoint to the Parasol application -- a feature that will help the business track claim volumes and categories.

You do not need to install anything. You do not need to file a ticket. Everything you need is already waiting for you on the platform.

== Learning objectives

By the end of this module, you will be able to:

* Navigate Developer Hub to discover applications, APIs, and documentation
* Use self-service templates to create an isolated development environment
* Write and test code in a cloud-based Dev Spaces IDE with AI assistance
* Push code through an automated CI/CD pipeline and resolve quality gate failures
* Verify a GitOps-driven deployment to production

[#section-1]
== Section 1: Meet the platform

Your first stop is Red Hat Developer Hub -- Parasol's internal developer portal. This is where developers discover services, explore APIs, read documentation, and get AI-assisted answers about the platform. Instead of asking colleagues where things are, you can find everything here.

=== Exercise 1: Explore Developer Hub

. Open **Red Hat Developer Hub** at {rhdh_url}[{rhdh_url}^]

. Log in with your credentials:
* Username: `{user}`
* Password: `{password}`

. Take a moment to look around the interface. Notice the left sidebar navigation:
* **Catalog** -- the software catalog where all applications, components, and APIs are registered
* **Create** -- self-service templates for provisioning new projects and environments
* **Lightspeed** -- an AI assistant that can answer questions about the platform

=== Exercise 2: Find the Parasol application in the software catalog

. Click **Catalog** in the left sidebar.

. Use the **Kind** dropdown to filter for **System** entities.

. Find and click on **parasol-insurance**.
+
This is the Parasol Insurance application system. It groups all the components, APIs, and resources that make up the application.

. Explore the system view:
* Notice the components registered under this system
* Look at the dependency relationships between components
* Click on a component to see its detail page

. On a component detail page, explore the tabs:
* **Overview** -- ownership, lifecycle, and system information
* **CI/CD** -- pipeline runs and build status
* **API** -- provided and consumed API definitions
* **Dependencies** -- what this component depends on and what depends on it

. Click the **API** tab and find the **parasol-insurance-api**. Click on it, then select the **Definition** tab.
+
The API documentation is rendered directly from the source repository. When the code changes, the docs stay current automatically -- no separate wiki to maintain.

// TODO: Add screenshot of Developer Hub catalog showing the Parasol Insurance system
// image::rhdh-catalog-parasol-system.png[Parasol Insurance system in the Developer Hub catalog,link=self,window=blank,width=700]

=== Exercise 3: Ask the AI assistant

Developer Hub includes an AI assistant called **Lightspeed** that can answer questions about your applications, APIs, and documentation.

. Click the **Lightspeed** icon in the bottom-right corner of the Developer Hub interface.

. Use the three dots menu in the top-right of the Lightspeed window to switch to full-screen view.

. Try asking a question about the Parasol application:
+
----
Tell me about the parasol-insurance-api API in the catalog
----

. Review the response. Lightspeed pulls information from the software catalog and documentation to give you contextual answers.

. Try another question:
+
----
What components make up the parasol-insurance system?
----

NOTE: Lightspeed responses may vary between attempts. If it does not find a specific entity on the first try, ask it to "list all APIs in the catalog" first, then re-ask your original question.

=== Verify

At this point, you should be able to:

* Log into Developer Hub
* Find the Parasol Insurance system in the software catalog
* View component details, CI/CD status, and API documentation
* Use Lightspeed to ask questions about the platform

'''

[#section-2]
== Section 2: Your mission

Now that you know your way around, it is time to get to work. You have been assigned a task: add a claims statistics API endpoint to the Parasol application. Before you start coding, you need to set up an isolated development environment with its own CI/CD pipeline.

=== Exercise 1: Create a development environment

Developer Hub provides self-service templates that let you provision environments without filing tickets. The platform team has created a template that sets up everything you need: a feature branch, a dedicated pipeline, and a catalog entry to track your work.

. In Developer Hub, click the **Create** button (the **+** icon) at the top of the page.

. Browse the available software templates. These are golden path templates maintained by the platform team.

. Select the **Parasol Insurance Development Environment** template (or similarly named template).

. Fill in the template form:
* **Branch name**: Enter a short name for your feature branch, for example: `claims-stats`
+
TIP: Keep the branch name short. It will be used to generate OpenShift Routes, which have length restrictions.

. Click **Create** and watch the provisioning progress:
* A feature branch is created in the source repository
* A CI/CD pipeline is configured for your branch
* A new component is registered in the Developer Hub catalog

. When provisioning completes, click **Open in Catalog** to see your new component.

// TODO: Add screenshot of the template provisioning results
// image::rhdh-template-results.png[Development environment provisioned with one click,link=self,window=blank,width=700]

=== Verify

You now have:

* A feature branch (`claims-stats`) in the Parasol source repository
* A CI/CD pipeline wired to your branch
* A catalog entry in Developer Hub tracking your development work

'''

[#section-3]
== Section 3: Build the feature

With your development environment ready, it is time to write code. You will use Red Hat OpenShift Dev Spaces -- a cloud-based IDE that runs entirely in the cluster. No local setup, no dependency conflicts, no "works on my machine" problems.

=== Exercise 1: Open Dev Spaces

. From your component page in Developer Hub, click the **Dev Spaces** link to open a cloud workspace for your feature branch.
+
Alternatively, you can open Dev Spaces directly at {devspaces_url}[{devspaces_url}^] and create a workspace from your repository URL.

. The first time Dev Spaces opens, you will see several authorization prompts. These are security gates that ensure the IDE only acts with your explicit consent:

.. **Authorize access to OpenShift** -- click **Allow selected permissions**
.. **Do you trust the authors of this repository?** -- click **Continue**
.. **GitLab login** -- log in with your credentials (`{user}` / `{password}`)
.. **Trust the workspace files** -- click **Yes, I trust the authors**
.. **Install extensions** -- click to install the recommended extensions including the AI coding assistant
.. Dismiss any popups about MicroProfile or Dependency Analytics by clicking the **X**
+
WARNING: If you miss the "trust the authors" prompt, Dev Spaces opens in Restricted Mode and you will see a highlighted banner at the bottom of the window. Click the banner to confirm trust so extensions load properly.

. Once the workspace loads, take a moment to explore:
* The file explorer shows the complete project structure
* The terminal has CLI tools pre-installed (`oc`, `mvn`, etc.)
* Extensions for Java, Quarkus, YAML, and the AI coding assistant are already configured
* Git is connected to your feature branch

// TODO: Add screenshot of the Dev Spaces workspace
// image::devspaces-workspace-loaded.png[Cloud development workspace with the Parasol Insurance project fully loaded,link=self,window=blank,width=700]

=== Exercise 2: Start the application in development mode

Before writing new code, start the application to see what you are working with.

. Press kbd:[Ctrl+Shift+P] (or kbd:[Cmd+Shift+P] on Mac) to open the command palette.

. Type and select **Tasks: Run Task**.

. Select the **devfile** task group.

. Select **Start Development Mode (Hot Reload + debug)**.

. Wait for the application to start. When Dev Spaces shows a popup notification to open the application, click to **open it in a new tab**.
+
TIP: If you miss the popup, open the application from the *ENDPOINTS* panel in the lower left of the Dev Spaces window. Find *quarkus-dev (8080/https)* and click the arrow icon.

. Browse the running Parasol Insurance application:
* This is a claims and customer management platform used by Parasol's agents
* It handles insurance claims processing and customer communication
* Behind the scenes, it uses Apache Kafka for message routing

// TODO: Add screenshot of the Parasol application
// image::parasol-app-home.png[Parasol Insurance application dashboard,link=self,window=blank,width=700]

=== Exercise 3: Add the claims statistics endpoint

Now implement the feature. You will create a new REST endpoint that returns claims statistics (totals, breakdowns by category and status).

. In the Explorer on the left, navigate to `src/main/java/com/parasol/`.

. Right-click on the `parasol` directory and select **New File**. Name it `ClaimsStatsResource.java`.

. Copy and paste the following code into the new file:
+
[source,java]
----
package com.parasol;

import java.util.HashMap;
import java.util.Map;

import jakarta.inject.Inject;
import jakarta.ws.rs.GET;
import jakarta.ws.rs.Path;
import jakarta.ws.rs.Produces;
import jakarta.ws.rs.core.MediaType;

@Path("/api/claims/stats")
@Produces(MediaType.APPLICATION_JSON)
public class ClaimsStatsResource {

    @Inject
    ClaimsResource claimsResource;

    @GET
    public Map<String, Object> getStats() {
        Map<String, Object> stats = new HashMap<>();

        System.out.println("Fetching claim statistics");

        try {
            var claims = claimsResource.getAllClaims();
            stats.put("total", claims.size());

            Map<String, Long> byCategory = new HashMap<>();
            for (var claim : claims) {
                byCategory.merge(claim.getCategory(), 1L, Long::sum);
            }
            stats.put("by_category", byCategory);

            Map<String, Long> byStatus = new HashMap<>();
            for (var claim : claims) {
                byStatus.merge(claim.getStatus(), 1L, Long::sum);
            }
            stats.put("by_status", byStatus);
        } catch (Exception e) {
        }

        return stats;
    }
}
----
+
NOTE: This code works, but it has some shortcuts. You used `System.out.println` instead of a proper logging framework, and the `catch` block silently swallows exceptions. These code smells are intentional -- the CI/CD pipeline will catch them in Section 4.

. The file saves automatically. Quarkus dev mode detects the change and reloads the application -- no manual build step required.

. In your browser, navigate to the running application and change the URL path to `/api/claims/stats`.
+
You should see JSON data with claim totals and breakdowns:
+
----
{
  "total": 15,
  "by_category": {
    "auto": 5,
    "home": 4,
    "life": 6
  },
  "by_status": {
    "open": 8,
    "closed": 7
  }
}
----

The new endpoint is working. You wrote the code, saved the file, and the application reloaded in seconds. No build commands, no repackaging, no redeployment.

// TODO: Add screenshot of the claims stats JSON response
// image::parasol-claims-stats-devmode.png[Claims statistics endpoint returning JSON data in dev mode,link=self,window=blank,width=700]

=== Verify

You now have:

* A running Parasol application in Quarkus development mode
* A new `/api/claims/stats` endpoint returning claims data
* Live reload working -- code changes are reflected in seconds

'''

[#section-4]
== Section 4: Ship it

Your feature works locally. Time to push it through the platform's CI/CD pipeline. A simple `git push` triggers an automated pipeline that builds, tests, and scans your code. No tickets, no manual triggers, no handoffs.

=== Exercise 1: Push your code

. In Dev Spaces, click the **Source Control** icon in the left sidebar (the branch icon).

. The new `ClaimsStatsResource.java` file appears under **Changes**.

. Click the **+** icon next to the file to stage it.

. Enter a commit message:
+
----
Add claims statistics API endpoint
----

. Click the dropdown arrow next to the **Commit** button and select **Commit & Push**.

// TODO: Add screenshot of the SCM commit view
// image::scm-commit.png[Developer commits and pushes code from the IDE,link=self,window=blank,width=700]

=== Exercise 2: Watch the pipeline in Developer Hub

. Switch back to **Developer Hub** in your browser.

. Navigate to your component in the catalog.

. Click the **CI/CD** tab to see the pipeline run that was triggered by your push.
+
The pipeline runs several stages:
+
* **Clone** -- source code pulled from the Git repository
* **SonarQube SAST** -- static analysis and code quality scanning
* **Build and Push** -- application compiled, container image built and pushed to the registry
* **Re-rollout app** -- updated application deployed to the dev environment

. Watch the pipeline progress. The **SonarQube SAST** stage will fail.

// TODO: Add screenshot of the pipeline running in Developer Hub
// image::ocp-pipeline-running.png[Pipeline run in Developer Hub showing stages in progress,link=self,window=blank,width=700]

=== Exercise 3: Diagnose the pipeline failure

The pipeline caught something. The SonarQube quality gate failed because of the code shortcuts you took earlier.

. Click on the failed pipeline run to see the details.

. Find the **SonarQube report URL** in the pipeline log output.

. Open the SonarQube report to see the specific issues:
* **`System.out.println` usage** -- standard output should not be used for logging in production code
* **Empty catch block** -- exceptions should not be silently swallowed
* **Field injection** -- SonarQube recommends constructor injection over `@Inject` field injection for better testability

These are not suggestions. They are hard quality gates. Code with these violations does not move forward.

// TODO: Add screenshot of the SonarQube report
// image::ocp-pipeline-sonar-report.png[SonarQube report showing code smell violations,link=self,window=blank,width=700]

=== Exercise 4: Fix the code with AI assistance

Instead of manually researching the Quarkus logging API and refactoring patterns, use the AI coding assistant that the platform team preconfigured in Dev Spaces.

. Switch back to **Dev Spaces**.

. Open the **Continue** AI assistant panel in the left sidebar.

. Enter this prompt:
+
----
The existing src/main/java/com/parasol/ClaimsStatsResource.java has
code smells according to SonarQube - can you fix them? The
System.out.println, field injection, and empty catch block. Don't
throw any new exceptions, just log it.
----
+
NOTE: If the AI response causes issues in the editor (a known issue with some models), open a new chat with the **+** button and try: "Please generate a fixed version of ClaimsStatsResource.java (do not try to edit it in place)." Then copy/paste the result.

. Review the changes the AI suggests:
* `System.out.println` replaced with `Log.info` from the Quarkus logging API
* Empty `try-catch` removed -- exceptions propagate naturally
* Field injection replaced with constructor injection

. Accept the changes.

. Verify the corrected code looks similar to this:
+
[source,java]
----
package com.parasol;

import java.util.HashMap;
import java.util.Map;

import io.quarkus.logging.Log;
import jakarta.ws.rs.GET;
import jakarta.ws.rs.Path;
import jakarta.ws.rs.Produces;
import jakarta.ws.rs.core.MediaType;

@Path("/api/claims/stats")
@Produces(MediaType.APPLICATION_JSON)
public class ClaimsStatsResource {

    private final ClaimsResource claimsResource;

    public ClaimsStatsResource(ClaimsResource claimsResource) {
        this.claimsResource = claimsResource;
    }

    @GET
    public Map<String, Object> getStats() {
        Map<String, Object> stats = new HashMap<>();

        Log.info("Fetching claim statistics");

        var claims = claimsResource.getAllClaims();
        stats.put("total", claims.size());

        Map<String, Long> byCategory = new HashMap<>();
        for (var claim : claims) {
            byCategory.merge(claim.getCategory(), 1L, Long::sum);
        }
        stats.put("by_category", byCategory);

        Map<String, Long> byStatus = new HashMap<>();
        for (var claim : claims) {
            byStatus.merge(claim.getStatus(), 1L, Long::sum);
        }
        stats.put("by_status", byStatus);

        return stats;
    }
}
----

. Commit and push the fix using the **Source Control** view:
* Stage the modified `ClaimsStatsResource.java`
* Commit message: `Fix code smells: use proper logging and remove empty catch block`
* Click the dropdown arrow next to **Commit** and select **Commit & Push**

=== Exercise 5: Verify the pipeline passes

. Switch back to **Developer Hub** and check the **CI/CD** tab.

. A new pipeline run starts automatically.

. Watch all stages complete successfully:
* The SonarQube quality gate passes this time
* The image is built and pushed
* The application is re-deployed

// TODO: Add screenshot of the pipeline passing
// image::ocp-pipeline-passed.png[All pipeline stages passing after fixing code smells,link=self,window=blank,width=700]

=== Verify

You have now:

* Pushed code and triggered an automated CI/CD pipeline
* Diagnosed a quality gate failure using SonarQube reports
* Used an AI assistant to fix code smells
* Pushed the fix and watched the pipeline pass

'''

[#section-5]
== Section 5: Go live

The pipeline passed. Your code has been built, tested, scanned, and packaged. Now it is time to merge your changes and see them deployed to production through GitOps.

=== Exercise 1: Merge your changes

. Open **GitLab** at {gitlab_url}[{gitlab_url}^] and log in with your credentials.

. Navigate to the Parasol Insurance repository.

. You should see a prompt to create a **Merge Request** from your feature branch. Click it.
+
If no prompt appears, click **Merge Requests** in the left sidebar, then **New Merge Request**, and select your branch.

. Review the merge request:
* The diff shows your new `ClaimsStatsResource.java` file with the corrected code
* The pipeline status shows green (all checks passed)

. Click **Merge** to merge your changes into the main branch.

=== Exercise 2: Watch GitOps deploy to production

. Open the **Argo CD console** at {argocd_url}[{argocd_url}^].
+
Log in with the credentials provided (check the workshop details page or the Terminal tab for the Argo CD password).

. Find the **parasol-insurance** application in the Argo CD dashboard.

. Watch the sync process:
* Argo CD detects that the desired state in Git has changed
* It reconciles the cluster to match the new state
* The application is deployed with your new feature

. Once the status shows **Synced** and **Healthy**, your feature is live.

// TODO: Add screenshot of Argo CD showing the synced application
// image::argocd-synced.png[Argo CD showing the Parasol application synced and healthy,link=self,window=blank,width=700]

=== Exercise 3: Verify the feature in production

. Find the Parasol application route in Developer Hub or the OpenShift Console.

. Open the production application and navigate to `/api/claims/stats`.

. Confirm the claims statistics endpoint is returning data in the production environment.

The feature you wrote, tested, fixed, and merged is now live -- without a single deployment ticket.

=== Exercise 4: Explore the OpenShift Console

For deeper debugging and monitoring, the OpenShift web console gives you direct access to the platform.

. Open the **OpenShift Console** at {console_url}[{console_url}^] (or use the tab in the right panel).

. Log in with your credentials.

. Navigate to the **Developer** perspective (toggle in the top-left if you are in Administrator view).

. Select the `{parasol_dev_ns}` namespace.

. Explore the available views:
* **Topology** -- visual representation of your application and its components
* **Observe** -- application metrics and dashboards
* **Project** -- resource usage and quotas

. Click on the Parasol application in the Topology view and explore:
* **Resources** -- pods, services, routes
* **Observe** -- CPU, memory, and network metrics
* Click **View logs** on a pod to see application logs directly

// TODO: Add screenshot of the OpenShift Console topology view
// image::ocp-topology.png[OpenShift Console showing the Parasol application topology,link=self,window=blank,width=700]

=== Verify

You have now:

* Merged your feature branch into main via GitLab
* Watched Argo CD deploy the changes to production automatically
* Verified the claims statistics endpoint is live in production
* Used the OpenShift Console for deeper application monitoring

'''

== Module summary

You just completed the full developer journey at Parasol Insurance -- from onboarding to production deployment -- without filing a single ticket, installing any tools, or waiting for anyone.

**What you accomplished:**

* Discovered applications, APIs, and documentation through Developer Hub's software catalog
* Used Lightspeed to get AI-powered answers about the platform
* Created an isolated development environment with a self-service template
* Wrote and tested code in a preconfigured cloud IDE with Dev Spaces
* Used an AI coding assistant to fix code quality issues
* Pushed code through an automated CI/CD pipeline with quality gates
* Deployed to production using GitOps with Argo CD
* Monitored the application using the OpenShift Console

**Key takeaways:**

* **Developer Hub** is the single pane of glass -- catalog, templates, CI/CD visibility, and AI assistance in one place
* **Dev Spaces** eliminates local environment setup -- every developer gets the same preconfigured workspace
* **Pipelines** enforce quality automatically -- code smells and security issues are caught before production
* **GitOps** makes deployments repeatable and auditable -- Git is the single source of truth
* **The platform** handles the undifferentiated heavy lifting so developers can focus on writing code

This is what a cohesive application platform looks like. Everything you need, from code to production, available on-premises, in any cloud, or at the edge.
